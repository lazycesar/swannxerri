{{ form_start(form, { 'attr' : { 'class': 'cta-ebook__form' } }) }}
    {{ form_widget(form) }}
    <button class="cta-ebook__submit">{{ button_label|default('Envoyer') }}</button>
{{ form_end(form) }}

