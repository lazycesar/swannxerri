{% extends 'front-base.html.twig' %}

{% block title %}Liste des articles{% endblock %}

{% block body %}

<main id="blog-article-page">   
        <section class="cover cover-single cover-blog" style="background-image: url({{ asset(blog.image) }})" >
            <div class="cover-single__text">
				<h1 class="page-single-h1">{{ blog.title }}</h1>
            </div>
        </section>     

        <div class="container-center cflex">
            <section class="mtb">                 
                <p>Ajouté le <strong> {{ blog.publicationDate ? blog.publicationDate|date('d/m/Y') : "" }} </strong></p>
                <p>{{ blog.content | raw }}</p>  

                <a href="{{ path('blog') }}">Retour</a>
                    
            </section>
        </div>{# end container center #} 
</main>

{% endblock %}